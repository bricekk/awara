import 'package:flutter/material.dart';

import 'package:awara/widgets/specialDeal.dart';

class SpecialDealSection extends StatelessWidget {
  const SpecialDealSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SpecialDealWidget();
  }
}
